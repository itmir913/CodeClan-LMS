{
  "name": "codeclan-lms",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "dev:vite": "wait-on tcp:8080 && npm run dev --prefix frontend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "concurrently --names \"VITE,AXUM\" --prefix-colors \"cyan,green\" \"npm run dev --prefix frontend\" \"cargo run --manifest-path src-tauri/Cargo.toml\"",
    "build": "npm run build --prefix frontend && tauri build",
    "build:frontend": "npm run build --prefix frontend",
    "build:backend": "cargo build --manifest-path src-tauri/Cargo.toml",
    "check": "cargo check --manifest-path src-tauri/Cargo.toml",
    "install:all": "npm install && npm install --prefix frontend",
    "install:frontend": "npm install --prefix frontend",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2",
    "concurrently": "^10.0.3",
    "wait-on": "^9.0.10"
  }
}
