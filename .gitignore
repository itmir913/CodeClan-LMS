# Rust / Cargo
/target/
src-tauri/target/
# Cargo.lock은 바이너리 프로젝트이므로 커밋에 포함 (제외하지 않음)

# Node / Frontend
node_modules/
frontend/node_modules/
frontend/dist/

# SQLite
*.db
*.db-wal
*.db-shm

# Tauri build artifacts
src-tauri/WixTools/
src-tauri/gen/

# IDE — IntelliJ / JetBrains
.idea/
*.iml
*.ipr
*.iws

# IDE — VS Code
.vscode/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Env / secrets
.env
.env.local
.env.*.local

# Cargo config (로컬 오버라이드용, 커밋 제외)
# .cargo/config.toml 은 빌드 픽스 목적으로 커밋에 포함
